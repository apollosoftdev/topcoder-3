{
  "name": "ai-arena",
  "version": "1.0.0",
  "description": "AI Arena Platform with Topcoder Authentication Integration",
  "author": "apollosoftdev",
  "license": "UNLICENSED",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/apollosoftdev/topcoder-3.git"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "node server.js",
    "dev:https": "USE_HTTPS=true node server.js",
    "build": "mvn clean package -DskipTests",
    "start": "mvn jetty:run -Djetty.port=8080",
    "test": "mvn test",
    "test:frontend": "cd src/main/webapp && npm test",
    "test:backend": "mvn test -Dtest=*Test",
    "docker:build": "DOCKER_BUILDKIT=1 docker build -t ai-arena .",
    "docker:run": "docker run -d --name ai-arena-app -p 8080:8080 -e ENVIRONMENT=development ai-arena",
    "docker:stop": "docker stop ai-arena-app 2>/dev/null; docker rm ai-arena-app 2>/dev/null; true",
    "docker:restart": "npm run docker:stop && npm run docker:build && npm run docker:run",
    "docker:logs": "docker logs -f ai-arena-app",
    "clean": "mvn clean",
    "ssl:generate": "mkdir -p ssl && openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout ssl/local.topcoder-dev.com.key -out ssl/local.topcoder-dev.com.crt -subj '/C=US/ST=State/L=City/O=Topcoder/CN=local.topcoder-dev.com' -addext 'subjectAltName=DNS:local.topcoder-dev.com,DNS:localhost'",
    "ssl:trust:mac": "sudo security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain ssl/local.topcoder-dev.com.crt",
    "ssl:trust:linux": "sudo cp ssl/local.topcoder-dev.com.crt /usr/local/share/ca-certificates/ && sudo update-ca-certificates",
    "ssl:setup:mac": "npm run ssl:generate && npm run ssl:trust:mac",
    "ssl:setup:linux": "npm run ssl:generate && npm run ssl:trust:linux",
    "hosts:add": "echo '127.0.0.1    local.topcoder-dev.com' | sudo tee -a /etc/hosts"
  },
  "keywords": [
    "ai-arena",
    "topcoder",
    "authentication",
    "jwt",
    "tc-auth-lib"
  ]
}
