# AI Arena Environment Configuration
# Copy this file to .env and configure for your environment

# =============================================================================
# ENVIRONMENT SETTINGS
# =============================================================================

# Environment mode: development | production
# - development: Uses topcoder-dev.com URLs
# - production: Uses topcoder.com URLs (default)
ENVIRONMENT=development

# =============================================================================
# AUTHENTICATION
# =============================================================================

# HS256 Secret for V2 token validation
# Required only if validating V2 (HS256) tokens
# Get this from Topcoder admin or use development test secret
TC_AUTH_SECRET=your-hs256-secret-here

# =============================================================================
# OPTIONAL OVERRIDES
# =============================================================================
# These can also be configured in src/main/resources/auth.properties

# Topcoder Auth0 Connector URL
# TC_AUTH_CONNECTOR_URL=https://accounts-auth0.topcoder-dev.com

# Topcoder Accounts URL
# TC_AUTH_ACCOUNTS_URL=https://accounts.topcoder-dev.com

# JWKS URL for RS256 token validation
# TC_AUTH_JWKS_URL=https://topcoder-dev.auth0.com/.well-known/jwks.json

# Token expiration offset in seconds (default: 60)
# TC_AUTH_TOKEN_OFFSET=60

# =============================================================================
# SERVER SETTINGS (if using embedded server)
# =============================================================================

# Server port
# SERVER_PORT=8080

# Server context path
# SERVER_CONTEXT_PATH=/
